spring:
  application:
    name: vuehr
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    name: defaultDataSource
    url: jdbc:mysql://47.111.3.127:3306/vhr?serverTimezone=UTC
    username: test
    password: test

mybatis:
  mapper-locations: classpath:mappers/*xml
  type-aliases-package: com.example.vuehr.mybatis.entity

server:
  port: 8080

